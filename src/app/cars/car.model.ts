export class Car {
  public brand;
  public model;
  public rentalOffice;
  constructor(brand, model, rentalOffice) {
    this.brand = brand;
    this.model = model;
    this.rentalOffice = rentalOffice;
  }
}
